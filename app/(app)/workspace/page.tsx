import SectionPage from "@/components/common/SectionPage";

export default function WorkspaceListPage() {
	return (
		<SectionPage
			title="Workspaces"
			description="Select a workspace or create a new one to begin planning."
		/>
	);
}
